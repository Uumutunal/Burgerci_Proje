
@model List<MenuViewModel>


@*Menüleri listele*@

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

<div class="container">

    @foreach (var item in Model)
    {
        <div class="card mb-3" style="max-width: 540px;">
            <div class="row g-0">
                <div class="col-md-4">
                    <img src="..." class="img-fluid rounded-start" alt="...">
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title">@item.Name</h5>


                        <form asp-action="AddToBasket" method="POST" style="display:inline">

                            <input type="hidden" name="Id" value="@item.Id" />
                            <input type="hidden" name="Name" value="@item.Name" />
                            <input type="hidden" name="HamburgerId" value="@item.HamburgerId" />
                            <input type="hidden" name="DrinkId" value="@item.DrinkId" />
                            <input type="hidden" name="HamburgerViewModel" value="@item.HamburgerViewModel" />

                            <button type="submit" class="btn btn-outline-primary btn-sm me-2">

                                <i class="bi bi-hand-thumbs-up"></i> Sepete Ekle

                            </button>

                        </form>

                    </div>
                </div>
            </div>
        </div>
    }


</div>

